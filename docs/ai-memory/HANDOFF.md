# Handoff

## Project

SolTides website

## Repo

- Public code repo: `SolTides/soltides-site-v2`
- Optional private memory repo: `SolTides/ai_memory`

## Current Status

- Variant-level inventory by vial size is live
- `SLP-3` now supports separate `60mg` and `10mg` inventory
- `60mg` uses live stock
- `10mg` is priced at `$30` and shows `Coming Soon` at zero stock

## Working Rules

- Prefer small targeted changes over large rewrites
- Treat the public repo plus these handoff files as the minimum source of truth
- If the private memory repo is cloned locally, it can be used as extra context
- Before publishing or deploying, summarize simply and ask for approval

## Likely Next Work

- QA the admin inventory flow
- QA the storefront product selection flow
- Continue polish after the inventory flow is confirmed solid
