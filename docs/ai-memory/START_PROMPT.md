# Start Prompt

Copy and paste this into a new Codex or ChatGPT coding chat:

```text
Use the SolTides public repo as memory for this session.

First, inspect the repo `SolTides/soltides-site-v2` and read these files in order:
1. `docs/ai-memory/LATEST.md`
2. `docs/ai-memory/HANDOFF.md`
3. `docs/ai-memory/README.md`

Treat those files as the public project memory source for this session.

Then inspect the current codebase and continue from the highest-priority open task.

If a local clone of the private repo `SolTides/ai_memory` is available on this machine, read that after the public handoff files for additional context, but do not require it.

While working:
- prefer the smallest effective change
- keep summaries simple
- update the public handoff files if the project status or next steps change
- before any publish or deploy action, summarize plainly and ask for approval
```
